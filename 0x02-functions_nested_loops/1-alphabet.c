#include "main.h"

/**
 *  *print_alphabet - prints the letters of alphabet in a lowercase
 *     * Return: void
 *      */
void print_alphabet(void)
{
		char count = 'a';

			for (count = 'a'; count < '{'; count++)
					{
								_putchar(count);
									}
				_putchar('\n');
}
